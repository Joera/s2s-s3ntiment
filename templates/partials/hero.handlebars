<section id="hero" class="container centered">  
        <article class="container container-large centered">
                <h2>{{stripHTML title}}</h2>
                <p><span>{{stripHTML content}}</span></p>
                <button class="btn-primary">Neem contact op</button>
        </article>
</section>