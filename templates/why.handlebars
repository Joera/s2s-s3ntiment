<!DOCTYPE html>
<html lang="{{language}}">
{{> head }}

<body id="page">
    {{> header}}
    <main id="page">
          
            <article class="page">   

                <section id="page-header">
                    <span class="tag">{{custom.slug}}</span>
                    <h2>{{title}}</h2> 
                </section> 

                {{{content}}}
                       
            </article>

            <section id="links" class="container container-large flex-centered flex-row">

                {{#each (prevNext collections.why position)}}
                    <article><h2>{{this.slug}}</h2><a href="{{this.path}}">Meer over {{this.slug}}</a></article>
                {{/each}}

            </section>
        
    </main>
    {{> footer }}
</body>
    {{> foot }} 
</html>